# Web Revision Desk

既存Webページを取り込み、見た目を確認しながら編集し、修正後HTML、視覚的な赤入れ、修正指示を作成するデスクトップ向けローカルWebアプリです。

[![CI](https://github.com/MZ-Gen-Labs/WebRevisionDesk/actions/workflows/ci.yml/badge.svg)](https://github.com/MZ-Gen-Labs/WebRevisionDesk/actions/workflows/ci.yml)
[![License](https://img.shields.io/badge/license-Apache--2.0-blue.svg)](LICENSE)

SingleFileを使わず、URLをElectron内蔵Chromiumで開いて現在の表示を単一HTMLとして取り込めます。外部ブラウザやlocalhost APIを必要としません。

## Electron版

v0.6.0以降はElectron版が標準です。Electron内蔵Chromiumでページ検索・プレビュー・取り込みを行い、案件フォルダはOS標準のフォルダ選択画面から利用します。

```bash
npm ci
npm run electron:dev
```

元の機能検証画面は`npm run electron:feasibility`で残しています。検証結果は[`docs/05-electron-feasibility.md`](docs/05-electron-feasibility.md)、移行内容は[`docs/06-electron-migration.md`](docs/06-electron-migration.md)を参照してください。

## できること

- ローカルHTMLの読込とsandbox内プレビュー
- ページ内要素の選択、テキスト編集
- 最大10件の検索・置換ルールを使った、候補ごとの確認・置換
- 表の新規作成、セル文章、行・列の追加・削除、表全体の削除
- リンクURL、画像、alt、CSS classの変更
- 要素の削除と同一親内での前後移動
- 選択した既存ブロックの複製
- 編集操作の変更履歴表示
- 変更前後をまとめた差分・修正指示HTMLの出力
- 元ページの見た目に取消し線・追加色・変更ラベルを重ねた赤入れHTMLの出力
- 修正前/修正後/変更箇所の切替と読込時点へのリセット
- 操作単位のUndo/Redo（ボタンおよびCtrl/Cmd+Z）
- `modified.html` のダウンロード
- 修正前・修正後・差分・赤入れ・案件情報のZIP一括保存
- 基準URL配下の複数ページを案件フォルダで階層管理
- 選択した要素に必要な項目だけを表示する初心者向け編集メニュー
- 未保存状態の表示と、ページ切替・アプリ終了時の自動保存

## 起動方法

利用者はGitHub ReleasesからWindows ZIPを取得し、展開後に`Start-WebRevisionDesk.cmd`を実行します。

ソースから起動する場合はNode.js 20以降を用意し、次を実行します。

```bash
npm ci
npm run electron:dev
```

## テスト

依存モジュール更新後を含む通常の確認は、次を実行します。

```bash
npm test
```

`npm test` は、URL階層、案件保存、検索・置換、差分生成、表編集、Windowsファイル名などの単体テストと本番ビルドを実行します。GitHub ActionsではPull Requestと`main`へのpushごとにWindows版Electronアーカイブの生成・起動スモークテストも実行します。

## バージョンとリリース

`package.json` をアプリのバージョン番号の正本とします。画面表示とWindows ZIP名はこの値を使用します。`npm run version:check` は `package.json`、`package-lock.json`、リリース時のGitタグが一致することを検証し、不一致の場合はCIとリリースを停止します。

通常の機能追加版を公開する手順は次のとおりです。

```bash
npm version minor --no-git-tag-version
npm test
git add package.json package-lock.json
git commit -m "Release vX.Y.Z"
git tag -a vX.Y.Z -m "Web Revision Desk vX.Y.Z"
git push origin main
git push origin vX.Y.Z
```

修正のみは `minor` を `patch`、互換性を壊す変更は `major` に置き換えます。タグのpushによりWindows版のビルド、整合性検査、GitHub Release公開が自動実行されます。テストに失敗した場合はタグを作成しません。

## 操作方法

案件選択・ログイン・補助的な取り込みは「取り込み・案件設定」を開いて操作します。ページ読込後はこの設定欄が自動で閉じ、編集領域が画面の残りの高さまで広がります。必要なときは見出しを押して再表示できます。修正前後・Undo/Redo・今すぐ保存・共有用保存は上部に常時表示します。

1. 案件フォルダを選び、左側のページ一覧から対象ページを開く
2. ページ上の直したい要素をクリックする
3. 右側の「編集メニュー」に表示された項目から内容を変更する
4. 文章はページ上でダブルクリックして直接編集することもできる
5. 「修正前」「修正後」「変更箇所」で表示を比較する
6. 「共有用ファイル一式を保存」で提出・保管用ZIPを、OSの保存画面から出力する
7. 修正後ページ、修正内容一覧、変更箇所ページだけが必要な場合は「その他の保存」から選び、OSの保存画面から出力する

「1つ戻す」「やり直す」で編集操作を段階的に戻せます。「すべてリセット」は全変更を読み込み時点へ戻します。ページ一覧で複数ページをチェックしている場合は、チェックした全ページの編集内容と変更履歴を取得時点へ戻します。取得済みデータと一覧は残ります。
通常は文章・リンク・画像差し替えだけが表示されます。画像のaltやCSS classを変更する場合は「詳細設定を表示」を有効にします。案件ページの変更は自動保存され、「自動保存待ち」「自動保存中」「自動保存済み」で状態を確認できます。「今すぐ保存」は自動保存を待たず確定したい場合に利用します。

### 複数ルールによる検索・置換

上部の「検索・置換」から最大10件のルールを設定できます。「修正前」「修正後」「変更箇所」のどの表示中でも開始でき、検索・置換を開くと自動的に「修正後・編集可能」へ切り替わります。ルールは上から順に実行され、該当箇所ごとに周辺文章、検索結果、置換後を見て「置換」「スキップ」を選べます。同じルールの残りだけを一括置換することもできます。上位ルールが置換した文字範囲は下位ルールでは再置換せず、赤色の除外対象として表示します。置換は通常の編集履歴へ記録されるため、Undo/Redo、変更箇所表示、自動保存の対象になります。

検索・置換ウィンドウは、上部の見出しをドラッグして移動できます。候補確認中と全ルール確認中は、ウィンドウ外の編集ページ上でマウスホイールを使ってページ全体をスクロールできます。候補確認中はページを見やすくするため自動的に横幅が小さくなり、背景の暗さを抑えます。薄い黄色は同じルールの全候補、濃いオレンジは現在の処理対象、赤い取消線は条件で除外された一致箇所です。候補は中央付近へスクロールされ、ウィンドウは候補と重ならない画面の隅へ自動移動します。一度選んだ位置は候補と重なるまで維持するため、近い候補の確認中にウィンドウが行き来しません。見出しへキーボードフォーカスを移した場合は、矢印キーでも位置を調整できます。

確認が不要な場合は「一括全置換（確認なし）」で現在のページへすべての有効なルールを直ちに適用できます。ページ一覧でチェックした保存済みページには「選択ページへ一括全置換」を使え、ページを順番に保存しながら適用状況と合計置換件数を表示します。

「全ルールの検出結果を確認」を使うと、置換を始める前に有効な全ルールを同時に判定し、ページ上の候補と除外箇所、ルール別件数をまとめて確認できます。上位ルールと重なる箇所は、ルール順に従って下位ルール側の除外として表示します。確認後は同じ画面から通常の上から順の置換確認へ進めます。

- 通常文字列または正規表現で検索する
- 検索範囲を「ページ全体」「本文を自動判定」「CSSセレクターで指定」から選ぶ
- 「リンク内の文字を除外」でハイパーリンク内の一致文字を置換対象外にする
- 「パンくず内の文字を除外」でパンくず領域だけを独立して置換対象外にする。パンくず内ではリンク除外設定を適用しない
- `$1`〜`$99`、`$&`を使って正規表現の一致内容を置換文へ利用する
- 検索文字列より前の必須条件・除外条件を、文字列同士の最大間隔とOR／ANDで判定する
- 検索文字列より後の除外条件を、文字列同士の最大間隔とOR／ANDで判定する
- 前方条件自体にも正規表現を利用する
- ルールの順序、使用・不使用、設定内容を端末へ保存する
- ルール一式をJSONファイルへ保存し、別の端末やバックアップから復元する

「条件をファイルに保存」では現在のルール一式をJSONファイルとして保存します。「条件をファイルから復元」では、そのJSONファイルを読み込んで現在のルールと置き換え、端末内にも保存します。

前後条件の「最大間隔」は、条件文字列そのものの長さを含みません。前方は「条件の末尾から検索文字列の先頭まで」、後方は「検索文字列の末尾から条件の先頭まで」に存在する文字数です。`0`は隣接、条件文字列と検索文字列の間に空白が1つある場合は間隔`1`として扱います。既存の保存済みルールは、従来の確認範囲から近い値へ自動移行されます。

「本文を自動判定」では、`article`、`main`、一般的な記事本文classなどを優先し、ヘッダー、ナビゲーション、フッター、サイド領域を除外します。サイト固有の構造へ厳密に合わせたい場合は、「CSSセレクターで指定」で `main .article-body` や `#contents` のように対象範囲を設定します。

検索文字がページにあっても全件が検索範囲外または除外条件に一致した場合、確認画面を閉じずに件数と理由を表示します。対象外の文字はページ上で赤色表示されるため、「文字はあるのに候補が出ない」場合の条件確認に利用できます。

「リンク内の文字を除外」を有効にすると、`<a>`要素内にある一致文字は置換せず、確認画面では赤色の除外対象として表示します。リンクURLやリンク文字列を意図せず変更したくないルールで利用できます。

パンくずはハイパーリンクかどうかでは判定せず、「パンくず内の文字を除外」だけで対象／対象外を決めます。この設定を無効にすると、リンクで構成されたパンくずも置換対象になります。`aria-label`、class、id、構造化データなどの一般的なパンくず表現（`breadcrumb`、`pankuzu`、`topicpath`など）を自動判定します。

### 表の作成・編集

右側の「表の作成・編集」で行数・列数と見出し行の有無を指定し、現在選択している要素の直後へ表を追加できます。通常は「詳細設定を表示」を有効にしたときだけ表示されますが、表・行・セルを選択している間は通常の編集項目として自動表示されます。表のセルを選択すると、選択行の下への行追加・行削除、選択列の右への列追加・列削除、表全体の削除が表示されます。セル内の文章は通常の文章と同様に右側の「文章」欄、またはセルのダブルクリックで編集できます。表操作は変更履歴、Undo/Redo、変更箇所表示、自動保存の対象です。

ローカルファイルで画像を差し替えると、画像データを再圧縮せずHTMLへ埋め込み、元のファイル名も保持します。変更箇所ページの画像変更一覧には、変更後画像をその元ファイル名で保存できるダウンロードボタンが表示されます。作業者は別の画像対応表を参照せず、Redline上の使用位置と画像を一緒に確認できます。

左パネルは `Alt/Option + ←` でページ一覧、`Alt/Option + →` で見出し構成へ切り替えられます。`Alt/Option + ↑ / ↓` で表示中リストを移動します。関連ページ検索や一括取得など時間のかかる処理中は、対象ボタンの名称を維持したまま明滅して進行中であることを示します。

SingleFile等で保存済みのHTMLを使用する場合は、「取り込み・案件設定」内の「その他の取り込み」を開きます。元のURLを入力して「保存済みHTMLを案件へ追加」を選ぶと、単独編集ではなく現在の案件ページとして保存されます。

## 案件フォルダで複数ページを管理

1. ChromeまたはEdgeで「案件フォルダを選択」を押す
2. 新規または既存の案件フォルダを選択する
3. 案件名と基準URLを入力する
4. 「関連ページを探す」を押す
5. 左側に表示された「未取得」ページを選ぶ
6. ログインや表示調整が必要な場合は、一覧の「取得用ブラウザで開く」から確認し、中央上部の「表示中ページを取り込む」を押す
7. 編集後に「案件フォルダに保存」を押す
8. 「保存済み」になったページは左側一覧から編集状態を再度開ける

一括検索は基準URLと同じドメインかつ配下パスだけを再帰的にたどり、アンカー違い・追跡用クエリ等を正規化して重複を除外します。画像、PDF、動画、ZIP等はページ候補に含めません。1回の検索は最大100ページで停止します。ログインや画面操作が必要なページは、一覧から選んで取得用ブラウザを開いた後に操作できます。

関連ページ検索で見つからないページは、「取り込み・案件設定」内の「その他の取り込み」でURLをページ一覧へ追加できます。URLを追加しただけではHTMLを保存せず、他の未取得ページと同じく画像プレビューから取り込みを判断できます。

基準URLが `https://example.com/products/widget` の場合、ページは次のように保存されます。

```text
案件フォルダ/
├── project.json
└── pages/products/widget/
    ├── index/
    │   ├── original.html
    │   ├── working.html
    │   ├── modified.html
    │   ├── diff.html
    │   ├── redline.html
    │   └── page.json
    └── function_const_01/
        └── ...
```

フォルダ名にはWindows 11で利用可能な文字だけを使用し、`project.json` と `page.json` のパスはOSに依存しない相対パスで記録します。編集途中の状態は `working.html`、提出可能な修正後ページは `modified.html` です。ページを切り替える際は未保存のページを案件フォルダへ自動保存します。

一括検索は、自身または所属組織が取得を許可されているサイトに対して使用してください。

### 一括取得と公開ページの更新確認

- 左側のチェックボックスで複数ページを選択する
- 「選択を一括取得」で未取得ページを順番に取得・保存する
- 未取得ページの本体をクリックすると、スクリーンショット取得後に完全なHTMLを取得し、案件フォルダへ保存して編集可能にする
- 「画像を更新」で公開ページの最新スクリーンショットを取得する。直近の画像は5分間だけメモリへ一時キャッシュされ、案件フォルダには保存しない
- ログインや画面操作が必要なページは、一覧下の「取得用ブラウザで開く」から確認後に取り込む
- 保存済みページを選び「選択を更新確認」で公開中ページとの差を確認する
- 「選択を未取得へ戻す」で、選択した保存済みページの原本・編集内容・差分・履歴・旧版を削除する。URLは一覧に残るため再取得できる
- 緑の「公開版と同じ」は取得時点と同じ状態
- オレンジの「公開版に更新あり」はテキスト、リンク、画像、構造、CSSのいずれかに差がある状態
- 「上書きしない」で案件内の編集を維持する
- 「バックアップして更新」で現在の一式を `versions/` へ退避してから公開版へ置き換える

更新確認だけでは `original.html` や編集中の内容を上書きしません。上書きを選択した場合も旧版はページフォルダ内の `versions/<日時>/` に保存されます。一括処理は同じログインプロファイルを使い、サーバー負荷を抑えるため1ページずつ順番に実行します。リセットは削除確認後に実行され、選択したページ自身の保存ファイルだけを対象とするため、URL階層上の子ページは保持されます。

## URLから取り込む

1. 関連ページ検索、または「その他の取り込み」の「URLをページ一覧へ追加」で対象URLを一覧へ追加する
2. 未取得ページの「取得用ブラウザで開く」を押す
3. 開いたChromiumで必要に応じてログイン、スクロール、タブ展開等を行う
4. 編集画面中央上部の「表示中ページを取り込む」を押す
5. CSS・画像等の埋め込み後、案件フォルダへ自動保存されたページの編集を開始する

通常の一覧クリックでは画像確認からHTML取得まで自動で進みます。JavaScript操作やログイン後の状態を反映して取り込みたい場合に「取得用ブラウザで開く」を使います。

取得用ブラウザのログイン状態はElectronの専用ユーザーデータ領域に保存され、外部サービスへは送信されません。Windowsでは通常`%APPDATA%\WebRevisionDesk`配下です。共有PCでは利用後のプロファイル管理に注意してください。

## アプリの設定

### ログインが必要なページの取得準備

案件フォルダを選択して基準URLを入力し、「このサイトはログインが必要」を有効にします。「ログイン用ブラウザを開く」で表示したブラウザ内でログイン・多要素認証を済ませ、対象ページを閲覧できることを確認してから、編集画面の「ログイン完了」を押してください。待機中は関連ページ検索、一括取得、公開ページ更新確認を開始できません。「ログイン待機を中止」で待機を取り消せます。

スイッチは選択中の案件へ保存されます。完了状態は画面を開いている間だけ保持し、案件の切り替えやURLの手入力変更で再確認待ちに戻ります。ログイン成功や期限切れの自動判定は行いません。認証が切れた場合は再度ログイン用ブラウザを開いてください。

完了時にブラウザを閉じ、専用プロファイルに残るCookie等を後続処理で使用します。ブラウザ終了時に失効する認証やsessionStorageのみを使うサイトでは、一括処理に認証を引き継げない場合があります。その場合は取得用ブラウザでログイン後、そのまま「表示中ページを取り込む」を利用してください。待機の中止はログアウトや認証情報の消去ではありません。専用プロファイルは案件間で共通です。

## Windows Electron版の作成

Windows 11またはWindowsのCI環境で次を実行します。

```powershell
npm ci
$env:WEB_REVISION_ELECTRON_RELEASE = "1"
npm run build:electron:win
```

`release-electron/` に次が生成されます。

- `WebRevisionDesk-<version>-electron-win-x64.zip`：Windows x64配布用
- `SHA256SUMS.txt`：配布物のSHA-256
- `release.json`：バージョンと配布ZIPのSHA-256を含む更新用メタデータ

ZIPには固定名の `WebRevisionDesk` フォルダが1つ含まれます。これを任意の場所へ展開し、`Start-WebRevisionDesk.cmd`を実行すると起動します。Windowsのポータブル版では起動時または「更新を確認」からGitHub Releaseの最新版を確認できます。更新ZIPはSHA-256を照合してからダウンロードされ、「再起動して更新」を選ぶと終了後に置換・再起動します。更新失敗時は既存フォルダを復元するため、元のバージョンを維持します。GitHub ReleaseにはZIP、`SHA256SUMS.txt`、`release.json`を添付します。

## 安全性と制約

編集プレビューはsandbox付きiframeに表示し、入力HTMLのスクリプト、フォーム送信、ポップアップを許可しません。ページ取り込みではCSS、CSS内の追加CSS、画像等を可能な範囲で単一HTMLへ埋め込みます。取得後も外部URLが残るページでは通信が発生する可能性があります。

ブラウザでHTMLを再解析するため、属性順序や空白等は元ファイルと同一にならない場合があります。iframe、Shadow DOM、canvas、疑似要素、JavaScriptが再生成する内容の編集は対象外です。

## 技術方針

VvvebJsではなく、`iframe + contenteditable + DOM API` を採用しています。今回の目的に不要なページビルダー機能を避け、SingleFile HTMLのDOMを直接扱い、出力内容を管理しやすくするためです。詳しい比較と受入条件は `docs/02-poc-implementation-specification.md` を参照してください。

PoC確認後の優先課題と実運用への進め方は `docs/03-pilot-and-production-plan.md` を参照してください。

## ライセンス

Apache License 2.0で公開しています。個人利用、社内利用、商用利用、改変、再配布が可能です。詳しくは `LICENSE` を参照してください。
